.screen-width#cover
  .image{:style => "background-image: url('" << @project.avatar_url.to_s << "');"}
.row.fix
  .col.s12
    %h2.shadow-header.center-align#title= @project.name
  .col.s12
    .center-align.fade#status
      - if @project.finished
        = @project.finish_status
        = t('project.finished')
      - else
        = @project.remain
        = t('project.remains')
.row.fix
  .col.s12.l8
    .card#project_page
      .card-stacked
        .card-content
          %p= to_markdown(@project.full_description)
        .card-action
          .row.fix
            .col.s12
              - if can?(:update, @project)
                %a.waves-effect.btn-flat.super-narrow.left{:href => edit_project_path}
                  %i.material-icons.hide-on-med-and-up#edit edit
                  %span.left.hide-on-small-and-down= t('.edit')
              .chip.fade.right= @project.user.full_name
              %a.right{:href => category_path(@project.category)}
                .chip.waves-effect.fade= @project.category.name
  .col.s12.l4
    .row.fix.hide-on-large-only
      .col.s12
        %h4= t('.invest')
    - [0.0001, 0.001, 0.01].each do |price|
      = render 'reward', project: @project, price: price, description: 'Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.'
.row.fix
  .col.s12
    %h4= t('.comments')
= commontator_thread(@project)
